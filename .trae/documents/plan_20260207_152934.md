# 高德地图 Key 替换与修复计划

我已收到你提供的新 Key (`827b6872ba0a28517707aac4c868be00`)。接下来的工作将确保这个 Key 正确应用到项目中，并清除旧的缓存。

## 📅 执行步骤

1.  **更新配置文件**
    *   修改 [AndroidManifest.xml](file:///c:/Users/Lenovo/Desktop/trip/android/app/src/main/AndroidManifest.xml) 文件。
    *   将旧的 Key `7b5299ccfa2d42185da30c989007c4e6` 替换为你提供的新 Key `827b6872ba0a28517707aac4c868be00`。

2.  **清理构建缓存**
    *   运行 `gradlew clean` 命令。
    *   这一步非常关键，能确保旧的错误配置被彻底清除，强制编译器读取新的 Key。

3.  **重新构建应用**
    *   运行 `gradlew assembleDebug` 命令。
    *   确保应用能成功编译，生成包含新 Key 的安装包。

## ✅ 预期结果
执行完上述步骤后，你的 APP 将具备正确的数字签名和对应的 Key，**“错误码 1008” 将彻底解决**，地图和定位功能将恢复正常。
